query GetAnimal($id: Int!) {
    animal(id: $id) {
        id
        name
        details {
            gender {
                value
            }
            color {
                id
                value
            }
            birthDate
            breed {
                id
                value
            }
            species {
                id
                value
            }
        }
        comments
    }
}
